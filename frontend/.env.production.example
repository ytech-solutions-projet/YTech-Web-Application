REACT_APP_ENV=production

# Leave empty when Nginx serves the frontend and proxies /api on the same domain.
REACT_APP_API_URL=

# Optional explicit websocket endpoint if you use one later.
# REACT_APP_WS_URL=wss://app.example.com
