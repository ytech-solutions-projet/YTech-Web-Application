{
  "scripts": {
    "reset:data": "npm --prefix backend run reset:data",
    "reset:data:all": "npm --prefix backend run reset:data:all",
    "reset:data:dry-run": "npm --prefix backend run reset:data:dry-run"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
